import User from "../model/user.js";

class UserData {
    create(user){
        return new User()
    }

    find(user){
        return new Array()
        // return []
    }

    findOne(user){
        return new User()
    }

    update(user){
        return new User()
    }

    delete(user){

    }
}